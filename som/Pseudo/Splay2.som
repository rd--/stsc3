Splay2 = PseudoUgen (
----------------------------

inArray: inArray = (
	^(Splay apply: {inArray. 1. 1. 0. true})
)

primaryFactoryMethod = (
	^#'inArray:'
)


comment = (^'
 Pan an array of channels across the stereo field *
')


)
