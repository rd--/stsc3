Adsr = PseudoUgen (
----------------------------

gate: gate attackTime: attackTime decayTime: decayTime sustainLevel: sustainLevel releaseTime: releaseTime curves: curves = (
	^(EnvAdsr apply: {attackTime. decayTime. sustainLevel. releaseTime. 1. curves}) asEnvGen: gate
)

primaryFactoryMethod = (
	^#'gate:attackTime:decayTime:sustainLevel:releaseTime:curves:'
)


comment = (^'
 EnvGen of simplified EnvAdsr *
')


)
