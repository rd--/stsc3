TChoose = PseudoUgen (
----------------------------

trig: trig array: array = (

^(Select apply: {(TiRand apply: {0. array size - 1. trig}). array})
)

primaryFactoryMethod = (

^#'trig:array:'
)


comment = (^'
 Randomly select one of several inputs on trigger *
')


)
