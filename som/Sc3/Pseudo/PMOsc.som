PMOsc = PseudoUgen (
----------------------------

carfreq: carfreq modfreq: modfreq pmindex: pmindex modphase: modphase = (

^(SinOsc apply: {carfreq. (SinOsc apply: {modfreq. modphase}) * pmindex})
)

primaryFactoryMethod = (

^#'carfreq:modfreq:pmindex:modphase:'
)


comment = (^'

')


)
