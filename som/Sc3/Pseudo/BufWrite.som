BufWrite = PseudoUgen (
----------------------------

bufnum: bufnum phase: phase loop: loop inputArray: inputArray = (

^(BufWr apply: {bufnum. phase. loop. inputArray})
)

primaryFactoryMethod = (

^#'bufnum:phase:loop:inputArray:'
)


comment = (^'

')


)
