CurveGen = PseudoUgen (
----------------------------

gate: gate levels: levels times: times curves: curves = (

^(EnvSpec apply: {{levels first} ++ levels. {0} ++ times. {0} ++ (curves extendCyclically: (times size)). nil. nil. 0}) asEnvGen: gate
)

primaryFactoryMethod = (

^#'gate:levels:times:curves:'
)


comment = (^'

')


)
