SelectX = PseudoUgen (
----------------------------

primaryFactoryMethod = (
	^#'which:array:'
)

which: which array: array = (
	^(XFade2 apply: {(Select apply: {which roundTo: 2. array}). (Select apply: {(which truncateTo: 2) + 1. array}). (which * 2 - 1) fold2: 1. 1})
)


comment = (^'
 Mix one output from many sources *
')


)
