BufAlloc = PseudoUgen (
----------------------------

numChannels: numChannels numFrames: numFrames = (

^(LocalBuf apply: {numChannels. numFrames})
)

primaryFactoryMethod = (

^#'numChannels:numFrames:'
)


comment = (^'

')


)
