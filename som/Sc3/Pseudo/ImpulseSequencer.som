ImpulseSequencer = PseudoUgen (
----------------------------

valueArray: valueArray trig: trig = (

^(Sequencer apply: {valueArray. trig}) * trig
)

primaryFactoryMethod = (

^#'valueArray:trig:'
)


comment = (^'

')


)
